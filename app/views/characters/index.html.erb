<h1>Characters</h1>


<% if @characters %>
    <%= link_to "Add Character", new_user_character_path(@creator)%><br><br>
    <% @characters.each do |char| %>
        <li>
            <%= link_to char.name, user_character_path(@creator, char) %>
        </li>
    <% end %>
<% end %>